**Task: Refine the "Projects" section of the Pineal Studio website.**

I want to redesign the Projects section to better showcase my portfolio.

### Requirements

1. **Replace the existing layout**

   * Remove the current **3 large project cards** completely.
   * Replace them with **9 project cards** arranged in a responsive grid.

2. **Card Design**

   * Make each project card **the same width and height as the Service cards** used elsewhere on the website.
   * Keep the design clean, premium, and consistent with the Pineal Studio branding.
   * Use rounded corners, subtle shadows, and smooth hover animations that match the rest of the site.
   * Ensure spacing between cards is consistent.

3. **Card Content**
   Each project card should contain:

   * Project Name
   * Website Category (Restaurant, Business, Portfolio, E-commerce, etc.)
   * A short **2-line description** explaining the website.
   * A "Visit Website" button or the entire card should be clickable.

4. **Website Links**

   * Each project card should support an external URL.
   * Use placeholder links for now (e.g., `https://example.com`) so I can replace them later.
   * Open links in a new tab (`target="_blank"` with proper `rel` attributes).

5. **Responsive Layout**

   * Desktop: 3 × 3 grid (3 columns, 3 rows).
   * Tablet: 2 columns.
   * Mobile: 1 column.
   * Maintain equal card heights on all screen sizes.

6. **Animations**

   * Cards should gently lift on hover.
   * Add a subtle glow or border accent on hover.
   * Keep animations smooth and premium, not flashy.

7. **Performance**

   * Use reusable components.
   * Keep the section lightweight and optimized.

8. **Code Quality**

   * Do not break any existing sections.
   * Keep styling consistent with the site's design system.
   * Follow the existing component structure and naming conventions.

The final Projects section should feel like a premium agency portfolio, allowing visitors to quickly browse multiple projects and click through to live websites.
